## Codebase Patterns
- Python source lives in `src/clawhawk/` with hatchling build backend
- Use `uv run pyright src/clawhawk/` for typechecking
- CLI entry point is `clawhawk.app:main`, also runnable via `python -m clawhawk`
- Dev dependencies go in `[dependency-groups] dev` section of pyproject.toml
- All public Pydantic models use `ConfigDict(alias_generator=to_camel, populate_by_name=True)` for camelCase JSON
- JSONL top-level fields are camelCase but `usage` sub-fields are snake_case — only `Message` needs alias_generator
- For stats parsing, use raw `json.loads` instead of Pydantic models for performance (only need type, timestamp, usage fields)
- Python `date.weekday()` returns 0=Monday (unlike Go where Sunday=0), so Monday-based week start = `now - timedelta(days=now.weekday())`
- WebSocket/API routes must be registered BEFORE the SPA catch-all `/{full_path:path}` route in app.py
- `_PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent` goes from `src/clawhawk/app.py` to project root

# Ralph Progress Log
Started: Fri 27 Mar 2026 18:30:05 AEDT
---

## 2026-03-27 - US-001
- Initialized Python project with uv: pyproject.toml with fastapi, uvicorn, websockets deps + pytest/pyright dev deps
- Created src/clawhawk/ package: __init__.py, __main__.py, app.py with minimal FastAPI app and CLI entry point
- Removed stale hello.py, updated .gitignore for Python artifacts
- Files changed: pyproject.toml, .gitignore, src/clawhawk/__init__.py, src/clawhawk/__main__.py, src/clawhawk/app.py
- **Learnings for future iterations:**
  - uv 0.5.22 is installed, uses hatchling build backend with `packages = ["src/clawhawk"]` for src layout
  - .python-version is set to 3.12, requires-python is >=3.11
  - uv.lock is auto-generated by uv sync — should be committed for reproducible builds
---

## 2026-03-27 - US-002
- Created src/clawhawk/models.py with all Pydantic data models
- Public models (camelCase JSON): Session (16 fields), ProjectGroup, TokenPeriod, TokenStats, DashboardMessage
- Internal JSONL parsing models: Message (with camelCase aliasing for sessionId/gitBranch), MessageContent, MessageUsage (snake_case for usage fields)
- All public models use ConfigDict(alias_generator=to_camel, populate_by_name=True)
- Files changed: src/clawhawk/models.py
- **Learnings for future iterations:**
  - JSONL files use mixed casing: top-level fields are camelCase (sessionId, gitBranch) but usage fields are snake_case (input_tokens, cache_creation_input_tokens)
  - Message model needs alias_generator=to_camel for JSONL parsing, but MessageContent/MessageUsage do NOT (their JSON keys are already snake_case)
  - Go Session struct has 16 fields (not 17 as PRD states) — the `version` field is included
  - `firstPrompt` is the Go JSON key (not `firstUserPrompt` as PRD notes suggest)
---

## 2026-03-27 - US-003
- Ported IDE lock file detection from Go to Python
- Created src/clawhawk/ide.py with `load_ide_map(ide_dir: str) -> dict[str, str]`
- Reads ~/.claude/ide/*.lock JSON files, maps workspaceFolders to ideName
- Malformed files logged as warnings and skipped gracefully
- Files changed: src/clawhawk/ide.py
- **Learnings for future iterations:**
  - IDE lock files have structure: `{"workspaceFolders": [...], "pid": int, "ideName": str}`
  - Go implementation silently skips errors; Python version logs warnings for malformed files
  - The ide_dir path is typically `~/.claude/ide`
---

## 2026-03-27 - US-004
- Ported core session parser and all helper functions from Go to Python
- Created src/clawhawk/sessions.py with: parse_session(), extract_user_text(), extract_action(), clean_command_text(), get_model_context_limit(), decode_project_path(), truncate()
- Also ported: _extract_tool_detail() for rich action descriptions, _truncate_keep_newlines() for last user prompt
- parse_session reads JSONL line-by-line using Pydantic Message model validation
- Files changed: src/clawhawk/sessions.py
- **Learnings for future iterations:**
  - Message.model_validate_json(line) works well for parsing JSONL — handles both camelCase aliases and defaults
  - extract_action returns the *last* tool_use or text part from assistant content arrays, matching Go behavior
  - Active status is NOT determined here — that's in the loading/grouping layer (US-005) which checks running PIDs
  - truncate() collapses newlines to spaces; _truncate_keep_newlines() preserves them (used only for last_user_prompt)
---

## 2026-03-27 - US-006
- Created src/clawhawk/stats.py with token usage aggregation
- `parse_file_token_stats(fpath)` reads JSONL, extracts assistant message usage grouped by date (YYYY-MM-DD)
- `load_token_stats()` globs all JSONL files, uses file-level cache with mod-time invalidation, aggregates into today/this_week/this_month
- Input tokens = input_tokens + cache_creation_input_tokens + cache_read_input_tokens (matching Go behavior)
- Files changed: src/clawhawk/stats.py
- **Learnings for future iterations:**
  - Used raw `json.loads` instead of Pydantic `Message.model_validate_json` for stats parsing — faster since we only need 3 fields (type, timestamp, usage)
  - Python's `datetime.fromisoformat()` handles RFC3339 timestamps natively (unlike Go which needs explicit format string)
  - Python `date.weekday()` returns 0=Monday, 6=Sunday — simpler than Go's Sunday=0 for Monday-based weeks
  - File-level cache pattern: `dict[str, _CachedFileStats]` with `os.stat().st_mtime` comparison, same as sessions.py
---

## 2026-03-27 - US-007
- Created FastAPI app with static file serving from web/dist/ and SPA fallback
- Static /assets/ mount via StaticFiles for hashed JS/CSS bundles
- Catch-all route serves exact files or falls back to index.html for SPA routing
- main() parses --port CLI arg (default 3333) and runs uvicorn
- __main__.py already calls main() from US-001
- Files changed: src/clawhawk/app.py
- **Learnings for future iterations:**
  - `_PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent` resolves from `src/clawhawk/app.py` to project root (works with editable installs via uv)
  - StaticFiles mount must come before catch-all route; mounting /assets/ separately avoids conflicts with the SPA fallback
  - The catch-all `/{full_path:path}` route handles both root-level static files (favicon.svg, icons.svg) and SPA client-side routing
  - WebSocket endpoint (US-008) must be registered BEFORE the catch-all route to avoid being caught by the SPA fallback
---
